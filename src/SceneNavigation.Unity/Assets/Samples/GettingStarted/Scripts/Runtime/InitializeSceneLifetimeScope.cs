using VContainer;
using VContainer.Unity;

namespace GettingStarted
{
    public class InitializeSceneLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}